/*
Arrow functions were introduced in ES6.
Arrow functions allow us to write shorter function syntax
*/

/* STRUCTURE:-

const fun_name = () => { body };
const fun_name = (argument) => { body };
const fun_name = (argument) => body ;

*/

const calculator = {
    addition: (a, b) => a + b,
    subtraction: (a, b) => a - b,
    multiplication: (a, b) => a * b,
    divided: (a, b) => a / b
};
